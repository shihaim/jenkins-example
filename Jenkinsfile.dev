pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh './gradlew clean'
                sh './gradlew build'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                sh 'cp ./build/libs/demo-0.0.1-SNAPSHOT.jar ./'
                sh 'java -jar demo-0.0.1-SNAPSHOT.jar'
            }
        }
    }
}